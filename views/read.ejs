<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-8 text-white">
        <a class="text-blue-300 hover:text-blue-500 text-xl" href="/">Back to home</a>
        <h1 class="py-4 tracking-tighter text-3xl mb-3">All users</h1>
        <div class="users flex gap-3 flex-wrap">
            <% if(users.length >0){ %>
                <% users.forEach(function(user){ %>
                    <div class="user w-70 p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-72 bg-zinc-500 rounded-lg overflow-hidden">
                            <img class="w-full h-full object-cover object-top"  src="<%= user.image %>" alt="">
                        </div>
                        <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
                        <h5 class="text-zinc-500"><%= user.email %></h5>
                        <div class="flex gap-3 place-content-between mt-4">
                            <a class="text-zinc-300 hover:text-zinc-400" href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-red-500 hover:text-red-700" href="/delete/<%= user._id %>">Delete User</a>
                        </div>
                    </div>
    
                <% }) %>
            <%} else { %>
                <h3 class="text-xl text-zinc-400">No Users Yet</h3>
           <% } %>


           
            
        </div>
        </form>
    </div>
</body>
</html>